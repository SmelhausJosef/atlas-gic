# Source Layout

## Active Runtime

The runnable MVP lives in `src/atlas/`.

## Legacy Prototypes

- `janus.py`
- `mirofish/*`

These files are intentionally left outside the active runtime path. They are reference material only and are not imported by the `atlas` package.

